/*! msal-node-wrapper v1.0.0-beta 2024-02-02 */
'use strict';
'use strict';

var Constants = require('../utils/Constants.js');

/*! @azure/msal-common v14.6.1 2024-01-23 */
/**
 * General error class thrown by the MSAL.js library.
 */
class AuthError extends Error {
    constructor(errorCode, errorMessage, suberror) {
        const errorString = errorMessage
            ? `${errorCode}: ${errorMessage}`
            : errorCode;
        super(errorString);
        Object.setPrototypeOf(this, AuthError.prototype);
        this.errorCode = errorCode || Constants.Constants.EMPTY_STRING;
        this.errorMessage = errorMessage || Constants.Constants.EMPTY_STRING;
        this.subError = suberror || Constants.Constants.EMPTY_STRING;
        this.name = "AuthError";
    }
    setCorrelationId(correlationId) {
        this.correlationId = correlationId;
    }
}

exports.AuthError = AuthError;
//# sourceMappingURL=AuthError.js.map
